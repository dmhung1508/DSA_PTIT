#include<bits/stdc++.h>
using namespace std;
int check1(int a,int b,int ax,int bx, int r){
    if (sqrt(   ))
}
int main(){
    int t;
    cin >> t;
    pair<int,int> a(t+1);
    for(int i =1; i<=t; i++){
        cin >> a[i].first >> a[i].second;
    }
    int ax,ay,bx,by;
    cin >> ax >> ay >> bx >> by;

}